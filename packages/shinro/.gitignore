node_modules
dist
# Written by the declaration-leak test, removed once it has read them.
dist-declarations
*.log
.DS_Store

# Type-checked scratch projects, removed by the tests that write them.
tests/.tmp-typecheck
